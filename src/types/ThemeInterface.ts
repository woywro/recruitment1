export interface ThemeInterface {
  colors: {
    primary: string;
    secondary: string;
    primaryBg: string;
    secondaryBg: string;
    primaryText: string;
    secondaryText: string;
    primaryActive: string;
    secondaryActive: string;
    gradient: string;
  };
  fontSizes: {
    small: string;
    medium: string;
    large: string;
  };
}
